@model IEnumerable<app_source.Models.CompanyModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoNegocio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaisBase)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ValorEstimado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EsTransacional)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoNegocio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaisBase)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValorEstimado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EsTransacional)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Nombre">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Nombre">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Nombre">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
